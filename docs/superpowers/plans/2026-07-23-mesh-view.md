# メッシュ表示 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `mat diag mesh` の JSON を mando が取得・正規化し、`/mesh` 独立ページで Thread メッシュのトポロジーとリンク品質を表示する。

**Architecture:** `[mesh]` config で exec するコマンドを与える（バックエンド非依存）。取得は実測 1 分 45 秒かかるので**非同期ジョブ**（`POST /api/mesh/refresh` で起動、`GET /api/mesh` が即答で状態＋スナップショットを返す）。mat 固有の知識は `normalize::normalize_mesh` に閉じ込め、フロントは等級（`grade`）だけを見る。UI は `mesh.html` をもう 1 枚 `include_str!` で焼き込む。

**Tech Stack:** Rust / axum / tokio / serde / toml、フロントは依存ライブラリなしの素の HTML + SVG。

## Global Constraints

- 設計: `docs/superpowers/specs/2026-07-23-mesh-view-design.md`
- issue: mando#5（本実装）、mat#13（自己同定の分裂・mat 側の責務）
- コミットメッセージ末尾に `（#5）` を付ける（このリポジトリの既存慣習）
- **実 IP・実 ext アドレス・実 alias をリポジトリに書かない。** テストとサンプルはダミー値を使う
- `cargo clippy -- -D warnings` が通ること
- `cargo test` が通ること
- 成果物はバイナリ 1 個 + `config.toml`（`index.html` / `mesh.html` は焼き込み）
- 既存の `index.html` は変更しない（`/mesh` へのリンクを張らない）
- 閾値の既定値: `lqi_fair = 2`、`lqi_weak = 1`、`fer_weak = 30`
- timeout の既定値: `ttl_ms = 600000`、`timeout_ms = 240000`

## 設計からの意図的な逸脱（1 点）

設計 §3 の `MeshView.nodes[].is_border_router` は**実装しない**。`mat diag mesh` の出力にボーダールータを示すフィールドはなく、実データで BR が判別できたのは利用者の `aliases.toml` が付けた `label` 文字列だけだった。ラベル文字列から BR を推定するのは mando が持つべきでない知識なので、`kind`（`ours` / `named` / `anonymous`）だけを持たせ、UI は `named`（＝他人だが名前が付いている）に ◆ を出す。実環境では BR がこれに当たるため見た目の結果は設計どおりになる。

設計 §3 の grade 判定順序も、そのままだと「FER が高い」規則が先に効いて `lqi == 0`（`bad`）を `weak` に**引き上げて**しまう。FER は品質を悪くする方向にしか働かないよう、LQI で等級を出してから FER で `weak` 以下へ落とす形に変える（Task 2 に反映済み）。

## File Structure

| ファイル | 責務 |
|---|---|
| `src/config.rs`（変更） | `[mesh]` セクションの型・既定値・検証 |
| `src/normalize.rs`（変更） | `normalize_mesh` — mat の生 JSON → `MeshView`。等級判定はここだけ |
| `src/main.rs`（変更） | ジョブ状態・`/mesh`・`/api/mesh`・`/api/mesh/refresh` |
| `mesh.html`（新規） | `/mesh` の画面。SVG 描画・ポーリング・表 |
| `config.example.toml`（変更） | `[mesh]` のサンプル |
| `CLAUDE.md`（変更） | 「やること（安定ミニ API）」に 3 endpoint を追記 |

---

## Task 1: config に `[mesh]` を足す

**Files:**
- Modify: `src/config.rs`
- Modify: `config.example.toml`

**Interfaces:**
- Consumes: なし
- Produces:
  - `config::Mesh { label: Option<String>, command: Vec<String>, ttl_ms: u64, timeout_ms: u64, thresholds: MeshThresholds, labels: Option<HashMap<String, String>> }`
  - `config::MeshThresholds { lqi_fair: u8, lqi_weak: u8, fer_weak: u8 }`（`Default` あり）
  - `Config.mesh: Option<Mesh>`
  - `ConfigError::EmptyMeshCommand`

- [ ] **Step 1: 失敗するテストを書く**

`src/config.rs` の `mod tests` の中、`health_empty_command_rejected` の直後に追加する。

```rust
    #[test]
    fn mesh_parses() {
        let p = write_tmp(
            "meshok",
            r##"
            [[device]]
            name = "s1"
            get_state = ["enl", "get", "x", "026301", "open_close_state"]
            open = ["enl", "set", "x", "026301", "open_close_operation", "open"]
            close = ["enl", "set", "x", "026301", "open_close_operation", "close"]
            [mesh]
            label      = "自宅メッシュ"
            command    = ["mat", "diag", "mesh"]
            ttl_ms     = 300000
            timeout_ms = 120000
            [mesh.thresholds]
            lqi_fair = 3
            lqi_weak = 2
            fer_weak = 10
            [mesh.labels]
            desk_light = "デスクライト"
            "##,
        );
        let cfg = Config::load(&p).unwrap();
        let m = cfg.mesh.as_ref().unwrap();
        assert_eq!(m.label.as_deref(), Some("自宅メッシュ"));
        assert_eq!(m.command, vec!["mat", "diag", "mesh"]);
        assert_eq!(m.ttl_ms, 300_000);
        assert_eq!(m.timeout_ms, 120_000);
        assert_eq!(m.thresholds.lqi_fair, 3);
        assert_eq!(m.thresholds.lqi_weak, 2);
        assert_eq!(m.thresholds.fer_weak, 10);
        assert_eq!(m.labels.as_ref().unwrap().get("desk_light").unwrap(), "デスクライト");
        std::fs::remove_file(p).ok();
    }

    #[test]
    fn mesh_defaults() {
        let p = write_tmp(
            "meshdefault",
            r##"
            [[device]]
            name = "s1"
            get_state = ["enl", "get", "x", "026301", "open_close_state"]
            open = ["enl", "set", "x", "026301", "open_close_operation", "open"]
            close = ["enl", "set", "x", "026301", "open_close_operation", "close"]
            [mesh]
            command = ["mat", "diag", "mesh"]
            "##,
        );
        let cfg = Config::load(&p).unwrap();
        let m = cfg.mesh.as_ref().unwrap();
        assert_eq!(m.label, None);
        assert_eq!(m.ttl_ms, 600_000);
        assert_eq!(m.timeout_ms, 240_000);
        assert_eq!(m.thresholds.lqi_fair, 2);
        assert_eq!(m.thresholds.lqi_weak, 1);
        assert_eq!(m.thresholds.fer_weak, 30);
        assert!(m.labels.is_none());
        std::fs::remove_file(p).ok();
    }

    #[test]
    fn mesh_absent_is_none() {
        let p = write_tmp(
            "meshnone",
            r##"
            [[device]]
            name = "s1"
            get_state = ["enl", "get", "x", "026301", "open_close_state"]
            open = ["enl", "set", "x", "026301", "open_close_operation", "open"]
            close = ["enl", "set", "x", "026301", "open_close_operation", "close"]
            "##,
        );
        let cfg = Config::load(&p).unwrap();
        assert!(cfg.mesh.is_none());
        std::fs::remove_file(p).ok();
    }

    #[test]
    fn mesh_empty_command_rejected() {
        let p = write_tmp(
            "meshempty",
            r##"
            [[device]]
            name = "s1"
            get_state = ["enl", "get", "x", "026301", "open_close_state"]
            open = ["enl", "set", "x", "026301", "open_close_operation", "open"]
            close = ["enl", "set", "x", "026301", "open_close_operation", "close"]
            [mesh]
            command = []
            "##,
        );
        assert!(matches!(
            Config::load(&p),
            Err(ConfigError::EmptyMeshCommand)
        ));
        std::fs::remove_file(p).ok();
    }
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `cargo test --lib config::tests::mesh_ -- --nocapture`
Expected: コンパイルエラー（`Config` に `mesh` フィールドが無い / `ConfigError::EmptyMeshCommand` が無い）

- [ ] **Step 3: 型と既定値を実装**

`src/config.rs` の `Health` 構造体の定義（`/// マシン健全性レポートのコマンド定義（任意）。` で始まるブロック）の直後に追加する。

```rust
/// Thread メッシュ表示のコマンド定義（任意）。未設定なら /mesh ごと無効。
///
/// `mat diag mesh` は全ノードを逐次 probe するので実測 1 分 45 秒（13 ノード）
/// かかる。graph / health の固定 30 秒とは別枠の timeout をここで持つ。
#[derive(Debug, Clone, Deserialize)]
pub struct Mesh {
    /// 画面見出しの対象名（任意。例 "自宅メッシュ"）。
    #[serde(default)]
    pub label: Option<String>,
    /// exec するコマンド配列。
    pub command: Vec<String>,
    /// スナップショットの鮮度。これより古い状態で /mesh を開くと自動で再取得する。
    #[serde(default = "default_mesh_ttl_ms")]
    pub ttl_ms: u64,
    /// exec 1 回の上限ミリ秒。超過は timeout 扱い。
    #[serde(default = "default_mesh_timeout_ms")]
    pub timeout_ms: u64,
    /// リンク品質の等級しきい値。
    #[serde(default)]
    pub thresholds: MeshThresholds,
    /// alias → UI 表示名の置換マップ（任意）。無いキーは alias を素通し。
    #[serde(default)]
    pub labels: Option<std::collections::HashMap<String, String>>,
}

/// リンク品質の等級しきい値。LQI と frame error rate の 2 軸で持つ —
/// 実測で LQI 1・誤り率 0% のリンクと LQI 1・誤り率 88% のリンクが併存し、
/// LQI だけでは弱いリンクを取りこぼすため。
#[derive(Debug, Clone, Deserialize)]
pub struct MeshThresholds {
    /// これ以下は fair。
    #[serde(default = "default_lqi_fair")]
    pub lqi_fair: u8,
    /// これ以下は weak（0 は bad）。
    #[serde(default = "default_lqi_weak")]
    pub lqi_weak: u8,
    /// frame error rate(%) がこれ以上なら LQI に関わらず weak 以下に落とす。
    #[serde(default = "default_fer_weak")]
    pub fer_weak: u8,
}

impl Default for MeshThresholds {
    fn default() -> Self {
        MeshThresholds {
            lqi_fair: default_lqi_fair(),
            lqi_weak: default_lqi_weak(),
            fer_weak: default_fer_weak(),
        }
    }
}

fn default_mesh_ttl_ms() -> u64 {
    600_000
}

fn default_mesh_timeout_ms() -> u64 {
    240_000
}

fn default_lqi_fair() -> u8 {
    2
}

fn default_lqi_weak() -> u8 {
    1
}

fn default_fer_weak() -> u8 {
    30
}
```

`Config` 構造体に `health` の直後でフィールドを足す。

```rust
    #[serde(default)]
    pub mesh: Option<Mesh>,
```

`ConfigError` の `EmptyHealthCommand` の直後に足す。

```rust
    EmptyMeshCommand,
```

`impl std::fmt::Display for ConfigError` の `ConfigError::EmptyHealthCommand => ...` の行の直後に足す。

```rust
            ConfigError::EmptyMeshCommand => write!(f, "mesh: command が空"),
```

`Config::validate` の `if let Some(h) = &self.health { ... }` ブロックの直後、`Ok(())` の直前に足す。

```rust
        if let Some(m) = &self.mesh {
            if m.command.is_empty() {
                return Err(ConfigError::EmptyMeshCommand);
            }
        }
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test --lib config::tests::mesh_`
Expected: 4 tests PASS（`mesh_parses` / `mesh_defaults` / `mesh_absent_is_none` / `mesh_empty_command_rejected`）

- [ ] **Step 5: `config.example.toml` にサンプルを足す**

ファイル末尾に追加する。

```toml

# ── メッシュ表示（[mesh]、任意）────────────────────────────
# Thread メッシュのトポロジーを /mesh で可視化する。未設定なら /mesh ごと 404。
#
# mat diag mesh は commission 済みノードを逐次 probe するので重い。
# 13 ノードで実測 1 分 45 秒（1 台の mDNS timeout を含む）。取得は非同期ジョブで
# 走り、GET /api/mesh は即答で状態とスナップショットを返す。
# 誰も /mesh を開いていなければ何も走らない（mando はスケジューラを持たない）。
#
# [mesh]
# label      = "自宅メッシュ"
# command    = ["mat", "diag", "mesh"]
# ttl_ms     = 600000        # 既定 600000（10 分）。これより古ければ開いた時に自動再取得
# timeout_ms = 240000        # 既定 240000（4 分）。graph/health の 30 秒とは別枠
#
# リンク品質の等級しきい値。LQI と frame error rate の 2 軸で持つ —
# LQI 1 でも誤り率 0% のリンクと 88% のリンクが併存するため、LQI だけでは
# 弱いリンクを取りこぼす。
# [mesh.thresholds]
# lqi_fair = 2               # 既定 2。これ以下は fair
# lqi_weak = 1               # 既定 1。これ以下は weak（0 は bad）
# fer_weak = 30              # 既定 30。誤り率(%) がこれ以上なら weak 以下へ落とす
#
# alias → 表示名の置換（[health.labels] と同型）。無いキーは alias を素通し。
# [mesh.labels]
# desk_light = "デスクライト"
```

- [ ] **Step 6: config.example.toml がパースできることを確認**

`config.example.toml` の `[mesh]` 節は全行コメントなので、既存の example パーステストがそのまま通る。

Run: `cargo test --lib config::tests`
Expected: 全 PASS（`example_config_parses` を含む）

- [ ] **Step 7: コミット**

```bash
git add src/config.rs config.example.toml
git commit -m "feat(config): [mesh] セクション（command / ttl / timeout / 閾値 / labels）を追加（#5）"
```

---

## Task 2: `normalize_mesh` — mat の JSON を安定形へ

**Files:**
- Modify: `src/normalize.rs`

**Interfaces:**
- Consumes: `config::MeshThresholds`
- Produces:
  - `normalize::MeshView { network: MeshNetwork, nodes: Vec<MeshNode>, edges: Vec<MeshEdge>, unidentified_count: usize, fetched_at: Option<String> }`
  - `normalize::MeshNetwork { name: Option<String>, channel: Option<u64>, leader_router_id: Option<u64>, split: bool }`
  - `normalize::MeshNode { id: String, name: String, kind: String, role: String, is_leader: bool, probed: Option<bool>, error_kind: Option<String> }`
  - `normalize::MeshEdge { a: String, b: String, grade: String, lqi: Option<u8>, rssi: Option<i64>, fer: Option<u8> }`
  - `pub fn normalize_mesh(raw: &Value, thresholds: &MeshThresholds, labels: Option<&HashMap<String, String>>) -> MeshView`

- [ ] **Step 1: 失敗するテストを書く**

`src/normalize.rs` の `mod tests` の末尾に追加する。

```rust
    fn th() -> crate::config::MeshThresholds {
        crate::config::MeshThresholds::default()
    }

    /// ext アドレスはすべてダミー（設置環境の実値は公開リポジトリに置かない）。
    fn sample() -> Value {
        json!({
          "timestamp": "2026-07-23T21:14:02+09:00",
          "network": {
            "name": "test-thread", "channel": 15,
            "partition_ids": [1581832881], "leader_router_id": 38
          },
          "nodes": [
            {"id": "ext:0011223344556677", "ext_address": "0011223344556677",
             "rloc16": "0x9800", "router_id": 38, "role": "leader"},
            {"id": "node:5", "node_id": 5, "alias": "desk_light",
             "role": "router", "probed": true},
            {"id": "ext:8899AABBCCDDEEFF", "ext_address": "8899AABBCCDDEEFF",
             "node_id": 7, "alias": "hall_light", "rloc16": "0x6c00",
             "router_id": 27, "role": "router", "probed": true},
            {"id": "ext:AABBCCDDEEFF0011", "ext_address": "AABBCCDDEEFF0011",
             "rloc16": "0x7c00", "router_id": 31, "role": "router",
             "label": "otbr-br"},
            {"id": "node:15", "node_id": 15, "role": "unknown", "probed": false,
             "probe_error": {"kind": "timeout", "detail": "no SRV+AAAA answer"}}
          ],
          "edges": [
            {"a": "node:5", "b": "ext:8899AABBCCDDEEFF",
             "a_sees_b": {"lqi": 3, "avg_rssi": -60, "last_rssi": -58,
                          "frame_error_rate": 0, "age": 12},
             "b_sees_a": {"lqi": 3, "avg_rssi": -62, "last_rssi": -61,
                          "frame_error_rate": 1, "age": 8}},
            {"a": "node:5", "b": "ext:AABBCCDDEEFF0011",
             "a_sees_b": null, "b_sees_a": null,
             "route": {"lqi_in": 3, "lqi_out": 3, "path_cost": 1}}
          ]
        })
    }

    #[test]
    fn mesh_network_and_counts() {
        let v = normalize_mesh(&sample(), &th(), None);
        assert_eq!(v.network.name.as_deref(), Some("test-thread"));
        assert_eq!(v.network.channel, Some(15));
        assert_eq!(v.network.leader_router_id, Some(38));
        assert!(!v.network.split);
        assert_eq!(v.fetched_at.as_deref(), Some("2026-07-23T21:14:02+09:00"));
        assert_eq!(v.nodes.len(), 5);
        assert_eq!(v.edges.len(), 2);
        // alias を持つのに ext を名乗れていないノード（node:5）だけを数える
        assert_eq!(v.unidentified_count, 1);
    }

    #[test]
    fn mesh_split_when_multiple_partitions() {
        let mut raw = sample();
        raw["network"]["partition_ids"] = json!([1, 2]);
        assert!(normalize_mesh(&raw, &th(), None).network.split);
    }

    #[test]
    fn mesh_node_kinds_and_names() {
        let v = normalize_mesh(&sample(), &th(), None);
        let by = |id: &str| v.nodes.iter().find(|n| n.id == id).unwrap();

        let leader = by("ext:0011223344556677");
        assert_eq!(leader.kind, "anonymous");
        assert_eq!(leader.name, "0011"); // ext の先頭 4 桁
        assert!(leader.is_leader);
        assert_eq!(leader.probed, None);

        let ours = by("node:5");
        assert_eq!(ours.kind, "ours");
        assert_eq!(ours.name, "desk_light"); // labels 未指定なので alias 素通し
        assert_eq!(ours.role, "router");
        assert_eq!(ours.probed, Some(true));
        assert!(!ours.is_leader);

        let named = by("ext:AABBCCDDEEFF0011");
        assert_eq!(named.kind, "named");
        assert_eq!(named.name, "otbr-br");

        let failed = by("node:15");
        assert_eq!(failed.kind, "anonymous"); // alias も label も無い
        assert_eq!(failed.probed, Some(false));
        assert_eq!(failed.error_kind.as_deref(), Some("timeout"));
    }

    #[test]
    fn mesh_labels_replace_alias() {
        let mut labels = std::collections::HashMap::new();
        labels.insert("desk_light".to_string(), "デスクライト".to_string());
        let v = normalize_mesh(&sample(), &th(), Some(&labels));
        let n = v.nodes.iter().find(|n| n.id == "node:5").unwrap();
        assert_eq!(n.name, "デスクライト");
    }

    #[test]
    fn mesh_route_only_edge() {
        let v = normalize_mesh(&sample(), &th(), None);
        let e = v.edges.iter().find(|e| e.b == "ext:AABBCCDDEEFF0011").unwrap();
        assert_eq!(e.grade, "route_only");
        assert_eq!(e.lqi, None);
        assert_eq!(e.rssi, None);
        assert_eq!(e.fer, None);
    }

    /// lqi だけで等級が決まるケース。
    #[test]
    fn mesh_grade_by_lqi() {
        for (lqi, want) in [(3u8, "good"), (2, "fair"), (1, "weak"), (0, "bad")] {
            let raw = json!({
              "network": {"partition_ids": []},
              "nodes": [{"id": "a"}, {"id": "b"}],
              "edges": [{"a": "a", "b": "b",
                         "a_sees_b": {"lqi": lqi, "avg_rssi": -70, "frame_error_rate": 0},
                         "b_sees_a": null}]
            });
            let v = normalize_mesh(&raw, &th(), None);
            assert_eq!(v.edges[0].grade, want, "lqi {lqi}");
        }
    }

    /// FER は等級を悪くする方向にしか働かない。good でも weak に落ち、bad は bad のまま。
    #[test]
    fn mesh_grade_fer_only_worsens() {
        let mk = |lqi: u8, fer: u8| {
            json!({
              "network": {"partition_ids": []},
              "nodes": [{"id": "a"}, {"id": "b"}],
              "edges": [{"a": "a", "b": "b",
                         "a_sees_b": {"lqi": lqi, "avg_rssi": -70, "frame_error_rate": fer},
                         "b_sees_a": null}]
            })
        };
        assert_eq!(normalize_mesh(&mk(3, 88), &th(), None).edges[0].grade, "weak");
        assert_eq!(normalize_mesh(&mk(2, 30), &th(), None).edges[0].grade, "weak");
        assert_eq!(normalize_mesh(&mk(0, 88), &th(), None).edges[0].grade, "bad");
        // しきい値未満の誤り率は等級を変えない
        assert_eq!(normalize_mesh(&mk(3, 29), &th(), None).edges[0].grade, "good");
    }

    /// 両視点のうち悪いほうを採る。LQI が同値なら誤り率が大きいほう。
    #[test]
    fn mesh_takes_worse_view() {
        let raw = json!({
          "network": {"partition_ids": []},
          "nodes": [{"id": "a"}, {"id": "b"}],
          "edges": [{"a": "a", "b": "b",
                     "a_sees_b": {"lqi": 3, "avg_rssi": -55, "frame_error_rate": 0},
                     "b_sees_a": {"lqi": 1, "avg_rssi": -94, "frame_error_rate": 5}}]
        });
        let e = &normalize_mesh(&raw, &th(), None).edges[0];
        assert_eq!(e.grade, "weak");
        assert_eq!(e.lqi, Some(1));
        assert_eq!(e.rssi, Some(-94));
        assert_eq!(e.fer, Some(5));

        let tie = json!({
          "network": {"partition_ids": []},
          "nodes": [{"id": "a"}, {"id": "b"}],
          "edges": [{"a": "a", "b": "b",
                     "a_sees_b": {"lqi": 2, "avg_rssi": -70, "frame_error_rate": 4},
                     "b_sees_a": {"lqi": 2, "avg_rssi": -72, "frame_error_rate": 40}}]
        });
        let e = &normalize_mesh(&tie, &th(), None).edges[0];
        assert_eq!(e.fer, Some(40));
        assert_eq!(e.grade, "weak"); // 誤り率 40 >= 30 で fair から落ちる
    }

    #[test]
    fn mesh_thresholds_move_the_boundary() {
        let raw = json!({
          "network": {"partition_ids": []},
          "nodes": [{"id": "a"}, {"id": "b"}],
          "edges": [{"a": "a", "b": "b",
                     "a_sees_b": {"lqi": 2, "avg_rssi": -70, "frame_error_rate": 0},
                     "b_sees_a": null}]
        });
        assert_eq!(normalize_mesh(&raw, &th(), None).edges[0].grade, "fair");
        let strict = crate::config::MeshThresholds { lqi_fair: 3, lqi_weak: 2, fer_weak: 30 };
        assert_eq!(normalize_mesh(&raw, &strict, None).edges[0].grade, "weak");
    }

    #[test]
    fn mesh_drops_edges_with_unknown_endpoint() {
        let raw = json!({
          "network": {"partition_ids": []},
          "nodes": [{"id": "a"}],
          "edges": [{"a": "a", "b": "ghost", "a_sees_b": null, "b_sees_a": null}]
        });
        assert!(normalize_mesh(&raw, &th(), None).edges.is_empty());
    }

    #[test]
    fn mesh_empty_graph() {
        let raw = json!({"network": {"partition_ids": []}, "nodes": [], "edges": []});
        let v = normalize_mesh(&raw, &th(), None);
        assert!(v.nodes.is_empty());
        assert!(v.edges.is_empty());
        assert!(!v.network.split);
        assert_eq!(v.network.name, None);
        assert_eq!(v.unidentified_count, 0);
        assert_eq!(v.fetched_at, None);
    }
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `cargo test --lib normalize::tests::mesh_`
Expected: コンパイルエラー（`normalize_mesh` が無い）

- [ ] **Step 3: 実装**

`src/normalize.rs` の `normalize_health_rows` 関数の直後（`#[cfg(test)]` の直前）に追加する。

```rust
/// メッシュの network セクション。
#[derive(Debug, Clone, PartialEq, Serialize)]
pub struct MeshNetwork {
    pub name: Option<String>,
    pub channel: Option<u64>,
    pub leader_router_id: Option<u64>,
    /// partition_ids が 2 つ以上 = メッシュが分裂している。
    pub split: bool,
}

/// メッシュの 1 頂点。
#[derive(Debug, Clone, PartialEq, Serialize)]
pub struct MeshNode {
    /// mat の安定キー（ext:… / node:… / rloc:…）をそのまま。
    pub id: String,
    /// UI 表示名。
    pub name: String,
    /// "ours"（自 fabric）| "named"（他人だが名前あり）| "anonymous"。
    pub kind: String,
    pub role: String,
    pub is_leader: bool,
    /// 自 fabric ノードのみが持つ。他人の参加者は probe していないので None。
    #[serde(skip_serializing_if = "Option::is_none")]
    pub probed: Option<bool>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub error_kind: Option<String>,
}

/// メッシュの 1 エッジ。lqi / rssi / fer は「悪いほうの視点」の値。
#[derive(Debug, Clone, PartialEq, Serialize)]
pub struct MeshEdge {
    pub a: String,
    pub b: String,
    /// "good" | "fair" | "weak" | "bad" | "route_only"。
    pub grade: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub lqi: Option<u8>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub rssi: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub fer: Option<u8>,
}

/// フロントが見る安定形。mat の生 JSON はここから先に出さない。
#[derive(Debug, Clone, PartialEq, Serialize)]
pub struct MeshView {
    pub network: MeshNetwork,
    pub nodes: Vec<MeshNode>,
    pub edges: Vec<MeshEdge>,
    /// 自 fabric なのに自分の ext を名乗れなかったノード数。
    /// 二重出現の注記を出すかの判断に使う（mat#13）。
    pub unidentified_count: usize,
    /// mat が付けた収集時刻。
    pub fetched_at: Option<String>,
}

/// 1 視点ぶんの neighbor 実測値。
#[derive(Clone, Copy)]
struct MeshLink {
    lqi: u8,
    rssi: i64,
    fer: u8,
}

fn read_link(v: &Value) -> Option<MeshLink> {
    let o = v.as_object()?;
    Some(MeshLink {
        lqi: o.get("lqi").and_then(Value::as_u64).unwrap_or(0) as u8,
        rssi: o.get("avg_rssi").and_then(Value::as_i64).unwrap_or(0),
        fer: o.get("frame_error_rate").and_then(Value::as_u64).unwrap_or(0) as u8,
    })
}

/// 等級の深刻度（大きいほど悪い）。
fn severity(grade: &str) -> u8 {
    match grade {
        "good" => 0,
        "fair" => 1,
        "weak" => 2,
        "bad" => 3,
        _ => 0,
    }
}

/// LQI で等級を出し、frame error rate が高ければ weak 以下へ落とす。
/// FER は悪くする方向にしか働かない（lqi 0 の bad を weak に引き上げない）。
fn grade_link(l: MeshLink, t: &crate::config::MeshThresholds) -> &'static str {
    let by_lqi = if l.lqi == 0 {
        "bad"
    } else if l.lqi <= t.lqi_weak {
        "weak"
    } else if l.lqi <= t.lqi_fair {
        "fair"
    } else {
        "good"
    };
    if l.fer >= t.fer_weak && severity(by_lqi) < severity("weak") {
        "weak"
    } else {
        by_lqi
    }
}

/// `mat diag mesh` の出力 JSON → フロント向けの安定形。
///
/// mat 固有の知識（`a_sees_b` / `b_sees_a` の構造、`ext:` / `node:` の ID 規約、
/// LQI が 0–3 スケールであること）はこの関数に閉じる（設計原則 4）。
///
/// エッジ品質は両視点のうち**悪いほう**を採る（弱点探しが目的なので楽観側を
/// 採らない）。悪さの比較は LQI 昇順、同値なら誤り率降順。
pub fn normalize_mesh(
    raw: &Value,
    thresholds: &crate::config::MeshThresholds,
    labels: Option<&std::collections::HashMap<String, String>>,
) -> MeshView {
    let net = raw.get("network");
    let partitions = net
        .and_then(|n| n.get("partition_ids"))
        .and_then(Value::as_array)
        .map(Vec::len)
        .unwrap_or(0);
    let network = MeshNetwork {
        name: net
            .and_then(|n| n.get("name"))
            .and_then(Value::as_str)
            .map(str::to_string),
        channel: net.and_then(|n| n.get("channel")).and_then(Value::as_u64),
        leader_router_id: net
            .and_then(|n| n.get("leader_router_id"))
            .and_then(Value::as_u64),
        split: partitions > 1,
    };

    let raw_nodes = raw
        .get("nodes")
        .and_then(Value::as_array)
        .cloned()
        .unwrap_or_default();

    let mut nodes = Vec::new();
    let mut unidentified_count = 0;
    for n in &raw_nodes {
        let Some(id) = n.get("id").and_then(Value::as_str) else {
            continue;
        };
        let alias = n.get("alias").and_then(Value::as_str);
        let label = n.get("label").and_then(Value::as_str);
        let (kind, name) = match (alias, label) {
            (Some(a), _) => {
                let shown = labels
                    .and_then(|m| m.get(a))
                    .map(String::as_str)
                    .unwrap_or(a);
                ("ours", shown.to_string())
            }
            (None, Some(l)) => ("named", l.to_string()),
            (None, None) => {
                let ext = n.get("ext_address").and_then(Value::as_str);
                let shown = match ext {
                    Some(e) if e.len() >= 4 => e[..4].to_string(),
                    Some(e) => e.to_string(),
                    None => id.to_string(),
                };
                ("anonymous", shown)
            }
        };
        // 自 fabric なのに ext を名乗れていない = mat#13 の二重出現候補。
        if alias.is_some() && id.starts_with("node:") {
            unidentified_count += 1;
        }
        let role = n
            .get("role")
            .and_then(Value::as_str)
            .unwrap_or("unknown")
            .to_string();
        nodes.push(MeshNode {
            id: id.to_string(),
            name,
            kind: kind.to_string(),
            is_leader: role == "leader",
            role,
            probed: n.get("probed").and_then(Value::as_bool),
            error_kind: n
                .get("probe_error")
                .and_then(|e| e.get("kind"))
                .and_then(Value::as_str)
                .map(str::to_string),
        });
    }

    let known: std::collections::HashSet<&str> = nodes.iter().map(|n| n.id.as_str()).collect();
    let mut edges = Vec::new();
    for e in raw
        .get("edges")
        .and_then(Value::as_array)
        .map(Vec::as_slice)
        .unwrap_or(&[])
    {
        let (Some(a), Some(b)) = (
            e.get("a").and_then(Value::as_str),
            e.get("b").and_then(Value::as_str),
        ) else {
            continue;
        };
        if !known.contains(a) || !known.contains(b) {
            continue;
        }
        let views: Vec<MeshLink> = ["a_sees_b", "b_sees_a"]
            .iter()
            .filter_map(|k| e.get(*k))
            .filter_map(read_link)
            .collect();
        // 悪いほう = LQI 昇順、同値なら誤り率降順。
        let worst = views
            .iter()
            .copied()
            .min_by(|x, y| x.lqi.cmp(&y.lqi).then(y.fer.cmp(&x.fer)));
        let edge = match worst {
            Some(l) => MeshEdge {
                a: a.to_string(),
                b: b.to_string(),
                grade: grade_link(l, thresholds).to_string(),
                lqi: Some(l.lqi),
                rssi: Some(l.rssi),
                fer: Some(l.fer),
            },
            None => MeshEdge {
                a: a.to_string(),
                b: b.to_string(),
                grade: "route_only".to_string(),
                lqi: None,
                rssi: None,
                fer: None,
            },
        };
        edges.push(edge);
    }

    MeshView {
        network,
        nodes,
        edges,
        unidentified_count,
        fetched_at: raw
            .get("timestamp")
            .and_then(Value::as_str)
            .map(str::to_string),
    }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test --lib normalize::tests::mesh_`
Expected: 11 tests PASS

- [ ] **Step 5: clippy を通す**

Run: `cargo clippy --all-targets -- -D warnings`
Expected: 警告なしで終了（exit 0）

- [ ] **Step 6: コミット**

```bash
git add src/normalize.rs
git commit -m "feat(normalize): normalize_mesh — mat diag mesh の JSON を安定形へ（#5）"
```

---

## Task 3: 非同期ジョブ API

**Files:**
- Modify: `src/main.rs`
- Modify: `CLAUDE.md`

**Interfaces:**
- Consumes: `config::Mesh`、`normalize::normalize_mesh` / `MeshView`
- Produces:
  - `GET /api/mesh` → `{ status, label?, stale, age_ms?, elapsed_ms?, snapshot?, error? }`
  - `POST /api/mesh/refresh` → 202（body なし）
  - どちらも `[mesh]` 未設定なら 404

- [ ] **Step 1: 失敗するテストを書く**

`src/main.rs` の `mod tests` に追加する。まず既存の `test_app()` の直後にヘルパを足す。

```rust
    /// 任意の config から App を組む（mesh のテストは config を差し替えたいので個別に作る）。
    fn app_from(toml_src: &str) -> Shared {
        let cfg: Config = toml::from_str(toml_src).unwrap();
        Arc::new(App {
            config: cfg,
            executor: Executor::new(),
            graph_executor: Executor::new(),
            mesh_executor: Executor::new(),
            state_cache: cache::Cache::default(),
            mesh_job: std::sync::Mutex::new(MeshJob::default()),
        })
    }

    /// 同じ App インスタンスに対して繰り返し叩く（ジョブ状態を跨いで見るため）。
    async fn call_on(app: Shared, method: &str, path: &str) -> (axum::http::StatusCode, Value) {
        let res = router(app)
            .oneshot(
                Request::builder()
                    .method(method)
                    .uri(path)
                    .body(Body::empty())
                    .unwrap(),
            )
            .await
            .unwrap();
        let status = res.status();
        let bytes = res.into_body().collect().await.unwrap().to_bytes();
        let json = serde_json::from_slice(&bytes).unwrap_or(Value::Null);
        (status, json)
    }

    /// status が running でなくなるまで待つ（最大 5 秒）。
    async fn settle(app: Shared) -> Value {
        for _ in 0..100 {
            let (_, v) = call_on(app.clone(), "GET", "/api/mesh").await;
            if v["status"] != "running" {
                return v;
            }
            tokio::time::sleep(std::time::Duration::from_millis(50)).await;
        }
        panic!("mesh job が 5 秒で終わらなかった");
    }

    const MESH_DEVICE: &str = r#"
        [[device]]
        name = "s1"
        get_state = ["sh", "-c", "printf '{}'"]
        open  = ["sh", "-c", "printf '{}'"]
        close = ["sh", "-c", "printf '{}'"]
    "#;

    /// 2 頂点 1 エッジの最小メッシュを返す偽コマンド（ext はダミー）。
    const MESH_OK_CMD: &str = r#"["sh", "-c", "printf '{\"timestamp\":\"t1\",\"network\":{\"name\":\"test-thread\",\"channel\":15,\"partition_ids\":[1],\"leader_router_id\":38},\"nodes\":[{\"id\":\"node:5\",\"node_id\":5,\"alias\":\"desk_light\",\"role\":\"router\",\"probed\":true},{\"id\":\"ext:0011223344556677\",\"ext_address\":\"0011223344556677\",\"role\":\"leader\"}],\"edges\":[{\"a\":\"node:5\",\"b\":\"ext:0011223344556677\",\"a_sees_b\":{\"lqi\":1,\"avg_rssi\":-94,\"frame_error_rate\":88},\"b_sees_a\":null}]}'"]"#;
```

続けてテスト本体を追加する。

```rust
    #[tokio::test]
    async fn mesh_api_404_without_config() {
        let app = app_from(MESH_DEVICE);
        let (s, _) = call_on(app.clone(), "GET", "/api/mesh").await;
        assert_eq!(s, StatusCode::NOT_FOUND);
        let (s, _) = call_on(app.clone(), "POST", "/api/mesh/refresh").await;
        assert_eq!(s, StatusCode::NOT_FOUND);
        let (s, _) = call_on(app, "GET", "/mesh").await;
        assert_eq!(s, StatusCode::NOT_FOUND);
    }

    #[tokio::test]
    async fn mesh_starts_empty_and_stale() {
        let app = app_from(&format!(
            "{MESH_DEVICE}\n[mesh]\nlabel = \"自宅メッシュ\"\ncommand = {MESH_OK_CMD}\n"
        ));
        let (s, v) = call_on(app, "GET", "/api/mesh").await;
        assert_eq!(s, StatusCode::OK);
        assert_eq!(v["status"], "empty");
        assert_eq!(v["label"], "自宅メッシュ");
        // 一度も取っていない = 取りに行くべき
        assert_eq!(v["stale"], true);
        assert!(v.get("snapshot").is_none());
    }

    #[tokio::test]
    async fn mesh_refresh_produces_snapshot() {
        let app = app_from(&format!(
            "{MESH_DEVICE}\n[mesh]\ncommand = {MESH_OK_CMD}\n"
        ));
        let (s, _) = call_on(app.clone(), "POST", "/api/mesh/refresh").await;
        assert_eq!(s, StatusCode::ACCEPTED);

        let v = settle(app).await;
        assert_eq!(v["status"], "idle");
        assert_eq!(v["stale"], false);
        assert!(v["age_ms"].as_u64().is_some());
        let snap = &v["snapshot"];
        assert_eq!(snap["network"]["name"], "test-thread");
        assert_eq!(snap["network"]["split"], false);
        assert_eq!(snap["nodes"].as_array().unwrap().len(), 2);
        assert_eq!(snap["unidentified_count"], 1);
        // lqi 1 かつ誤り率 88% → weak
        assert_eq!(snap["edges"][0]["grade"], "weak");
        assert_eq!(snap["fetched_at"], "t1");
    }

    #[tokio::test]
    async fn mesh_refresh_is_single_flight() {
        // 走った回数をファイル行数で数える。
        let counter = std::env::temp_dir().join(format!(
            "mando-mesh-singleflight-{}",
            std::process::id()
        ));
        std::fs::remove_file(&counter).ok();
        let cmd = format!(
            r#"["sh", "-c", "echo x >> {}; sleep 0.4; printf '{{\"network\":{{\"partition_ids\":[1]}},\"nodes\":[],\"edges\":[]}}'"]"#,
            counter.display()
        );
        let app = app_from(&format!("{MESH_DEVICE}\n[mesh]\ncommand = {cmd}\n"));

        for _ in 0..3 {
            let (s, _) = call_on(app.clone(), "POST", "/api/mesh/refresh").await;
            assert_eq!(s, StatusCode::ACCEPTED);
        }
        let v = settle(app).await;
        assert_eq!(v["status"], "idle");

        let runs = std::fs::read_to_string(&counter).unwrap().lines().count();
        assert_eq!(runs, 1, "同時 refresh でコマンドが複数回走った");
        std::fs::remove_file(&counter).ok();
    }

    #[tokio::test]
    async fn mesh_reports_command_failure() {
        let app = app_from(&format!(
            "{MESH_DEVICE}\n[mesh]\ncommand = [\"sh\", \"-c\", \"exit 1\"]\n"
        ));
        call_on(app.clone(), "POST", "/api/mesh/refresh").await;
        let v = settle(app).await;
        assert_eq!(v["status"], "failed");
        assert_eq!(v["error"], "failed");
        assert!(v.get("snapshot").is_none());
    }

    #[tokio::test]
    async fn mesh_reports_bad_json() {
        let app = app_from(&format!(
            "{MESH_DEVICE}\n[mesh]\ncommand = [\"sh\", \"-c\", \"printf 'not-json'\"]\n"
        ));
        call_on(app.clone(), "POST", "/api/mesh/refresh").await;
        let v = settle(app).await;
        assert_eq!(v["status"], "failed");
        assert_eq!(v["error"], "bad_json");
    }
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `cargo test --lib main::tests::mesh_ 2>&1 | head -30`
Expected: コンパイルエラー（`MeshJob` が無い / `App` に `mesh_executor`・`mesh_job` が無い）

> 注: テストモジュールのパスはクレート構成により `tests::mesh_...` になる。落ちることが確認できればよいので `cargo test mesh_` で代用してよい。

- [ ] **Step 3: App にジョブ状態を足す**

`src/main.rs` の `use` に足す。

```rust
use normalize::MeshView;
```

`struct App` の `state_cache` の直後にフィールドを足す。

```rust
    /// mesh 取得専用の直列化器。mat diag mesh は matd を経由しない直経路で
    /// 重い（実測 1 分 45 秒）ため、devices / graph とは別枠にする。
    mesh_executor: Executor,
    /// mesh 取得ジョブの状態とスナップショット（1 スロット・メモリ内）。
    /// 永続化しない — mando 再起動で empty に戻る。
    mesh_job: std::sync::Mutex<MeshJob>,
```

`struct App` の直前に `MeshJob` を定義する。

```rust
/// mesh 取得ジョブの状態。running 中は running_since が Some。
#[derive(Default)]
struct MeshJob {
    running_since: Option<std::time::Instant>,
    /// 直近に成功した取得（取得完了時刻 + 正規化済みビュー）。
    snapshot: Option<(std::time::Instant, MeshView)>,
    /// 直近の取得が失敗したときの機械可読な理由。成功時にクリアする。
    error: Option<String>,
}
```

`impl App` に足す。

```rust
    /// mesh スナップショットの鮮度上限（config の [mesh] ttl_ms）。
    fn mesh_ttl(&self) -> std::time::Duration {
        std::time::Duration::from_millis(
            self.config.mesh.as_ref().map(|m| m.ttl_ms).unwrap_or(0),
        )
    }
```

`main()` の `App { ... }` 構築に足す。

```rust
        mesh_executor: Executor::new(),
        mesh_job: std::sync::Mutex::new(MeshJob::default()),
```

- [ ] **Step 4: ハンドラとルートを実装**

`src/main.rs` の `health_unavailable` 関数の直後（`#[cfg(test)]` の直前）に追加する。

```rust
/// GET /api/mesh の応答。running 中でも前回スナップショットは返す
/// （UI が薄く表示したまま更新できるように）。
#[derive(Serialize)]
struct MeshStatusView {
    /// "empty" | "running" | "idle" | "failed"
    status: &'static str,
    #[serde(skip_serializing_if = "Option::is_none")]
    label: Option<String>,
    /// true なら UI は取得を仕掛けてよい（未取得 or ttl 超過）。
    stale: bool,
    #[serde(skip_serializing_if = "Option::is_none")]
    age_ms: Option<u64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    elapsed_ms: Option<u64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    snapshot: Option<MeshView>,
    #[serde(skip_serializing_if = "Option::is_none")]
    error: Option<String>,
}

/// [mesh] 未設定 → 404。
fn mesh_not_configured() -> Response {
    (
        StatusCode::NOT_FOUND,
        [(header::CONTENT_TYPE, "application/json")],
        r#"{"error":"mesh not configured"}"#.to_string(),
    )
        .into_response()
}

async fn get_mesh(State(app): State<Shared>) -> Response {
    let Some(mesh) = &app.config.mesh else {
        return mesh_not_configured();
    };
    let job = app.mesh_job.lock().expect("mesh_job poisoned");
    let age_ms = job.snapshot.as_ref().map(|(at, _)| at.elapsed().as_millis() as u64);
    let status = if job.running_since.is_some() {
        "running"
    } else if job.error.is_some() {
        "failed"
    } else if job.snapshot.is_some() {
        "idle"
    } else {
        "empty"
    };
    // 取得中は「さらに仕掛けろ」と言わない。
    let stale = job.running_since.is_none()
        && match job.snapshot.as_ref() {
            None => true,
            Some((at, _)) => at.elapsed() >= app.mesh_ttl(),
        };
    Json(MeshStatusView {
        status,
        label: mesh.label.clone(),
        stale,
        age_ms,
        elapsed_ms: job.running_since.map(|t| t.elapsed().as_millis() as u64),
        snapshot: job.snapshot.as_ref().map(|(_, v)| v.clone()),
        error: job.error.clone(),
    })
    .into_response()
}

/// 取得ジョブを起動する。既に走っていれば何もせず 202（single-flight）。
async fn refresh_mesh(State(app): State<Shared>) -> Response {
    if app.config.mesh.is_none() {
        return mesh_not_configured();
    }
    {
        let mut job = app.mesh_job.lock().expect("mesh_job poisoned");
        if job.running_since.is_some() {
            return StatusCode::ACCEPTED.into_response();
        }
        job.running_since = Some(std::time::Instant::now());
    }
    let spawned = app.clone();
    tokio::spawn(async move { run_mesh_job(spawned).await });
    StatusCode::ACCEPTED.into_response()
}

/// exec → パース → 正規化 → ジョブスロットへ格納。失敗理由は機械可読な
/// 文字列で残し、日本語化は UI の責務にする。
async fn run_mesh_job(app: Shared) {
    let mesh = app
        .config
        .mesh
        .as_ref()
        .expect("run_mesh_job は [mesh] 有りでのみ起動する");
    let timeout = std::time::Duration::from_millis(mesh.timeout_ms);
    let result = run_bounded(&app.mesh_executor, "mesh", &mesh.command, timeout).await;

    let outcome = if result.outcome != ExecOutcome::Success {
        tracing::warn!(
            outcome = ?result.outcome,
            stderr = %result.stderr.trim(),
            "mesh 取得が非成功"
        );
        Err(match result.outcome {
            ExecOutcome::Timeout => "timeout",
            ExecOutcome::Rejected => "rejected",
            ExecOutcome::NetworkError => "network_error",
            ExecOutcome::SpawnFailed => "spawn_failed",
            _ => "failed",
        })
    } else {
        match serde_json::from_str::<Value>(&result.stdout) {
            Ok(raw) => Ok(normalize::normalize_mesh(
                &raw,
                &mesh.thresholds,
                mesh.labels.as_ref(),
            )),
            Err(_) => {
                tracing::warn!("mesh の stdout が JSON でない");
                Err("bad_json")
            }
        }
    };

    let mut job = app.mesh_job.lock().expect("mesh_job poisoned");
    job.running_since = None;
    match outcome {
        Ok(view) => {
            job.error = None;
            job.snapshot = Some((std::time::Instant::now(), view));
        }
        Err(kind) => job.error = Some(kind.to_string()),
    }
}
```

`router()` に 2 行足す（`/api/health` の直後）。

```rust
        .route("/api/mesh", get(get_mesh))
        .route("/api/mesh/refresh", post(refresh_mesh))
```

> `/mesh` ページ本体は Task 4 で足す。このタスクの `mesh_api_404_without_config` は
> `/mesh` にも 404 を期待しているので、Task 3 では暫定で下の行も足しておく
> （Task 4 で `mesh_page` の中身を実装する）。

```rust
        .route("/mesh", get(mesh_page))
```

暫定の `mesh_page`（Task 4 で本実装に置き換える）を `get_mesh` の直前に置く。

```rust
async fn mesh_page(State(app): State<Shared>) -> Response {
    if app.config.mesh.is_none() {
        return mesh_not_configured();
    }
    Html("").into_response()
}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `cargo test mesh_`
Expected: Task 2 の 11 件 + このタスクの 6 件が PASS

- [ ] **Step 6: 全テストと clippy**

Run: `cargo test && cargo clippy --all-targets -- -D warnings`
Expected: すべて PASS、警告なし

- [ ] **Step 7: `CLAUDE.md` の「やること（安定ミニ API）」を更新**

`- \`GET  /api/health\` — ...` の行の直後に追加する。

```markdown
- `GET  /mesh` — 焼き込んだ `mesh.html` を返す（`[mesh]` 未設定なら 404）
- `GET  /api/mesh` — 取得ジョブの状態とスナップショット（即答）。`{ "status": "empty|running|idle|failed", "stale": bool, "snapshot"?: {...} }`
- `POST /api/mesh/refresh` — mesh 取得ジョブを起動（202・single-flight）
```

- [ ] **Step 8: コミット**

```bash
git add src/main.rs CLAUDE.md
git commit -m "feat(api): mesh 取得の非同期ジョブと /api/mesh を追加（#5）"
```

---

## Task 4: `/mesh` 画面

**Files:**
- Create: `mesh.html`
- Modify: `src/main.rs`

**Interfaces:**
- Consumes: `GET /api/mesh`（Task 3）
- Produces: `GET /mesh` が `mesh.html` を返す

- [ ] **Step 1: `mesh.html` を作る**

リポジトリルートに `mesh.html` を新規作成する。全文:

```html
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>メッシュ — mando</title>
<style>
  :root {
    --bg: #0b1020; --panel: #151b32; --line: #263154; --fg: #e8ecf8; --dim: #8b96b8;
    --good: #45c17a; --fair: #e0a53a; --weak: #e4604f; --bad: #a02a1f; --route: #3b4770;
    --accent: #6aa9ff;
  }
  * { box-sizing: border-box; }
  body { margin: 0; background: var(--bg); color: var(--fg);
         font: 15px/1.5 -apple-system, "Hiragino Kaku Gothic ProN", "Noto Sans JP", sans-serif; }
  header { padding: 14px 16px 10px; border-bottom: 1px solid var(--line); }
  h1 { font-size: 17px; margin: 0 0 8px; font-weight: 700; }
  .netinfo { display: flex; flex-wrap: wrap; gap: 6px 14px; font-size: 13px; color: var(--dim); }
  .netinfo b { color: var(--fg); font-weight: 600; }
  .warn { color: var(--fair); }
  main { padding: 12px 16px 60px; max-width: 760px; margin: 0 auto; }
  .bar { display: flex; flex-wrap: wrap; gap: 10px; align-items: center; margin: 12px 0; }
  button.act { background: var(--accent); color: #0b1020; border: 0; border-radius: 999px;
               padding: 9px 18px; font: inherit; font-weight: 700; cursor: pointer; }
  button.act[disabled] { background: #2a3150; opacity: .5; cursor: default; }
  .chk { display: inline-flex; align-items: center; gap: 6px; font-size: 13px; color: var(--dim); }
  .stamp { margin-left: auto; font-size: 12px; color: var(--dim); }
  .card { background: var(--panel); border: 1px solid var(--line); border-radius: 14px;
          padding: 10px; margin: 12px 0; position: relative; }
  .card.stale svg { opacity: .35; }
  .overlay { position: absolute; inset: 0; display: flex; align-items: center;
             justify-content: center; text-align: center; font-size: 13px; padding: 16px; }
  svg { width: 100%; height: auto; display: block; }
  .legend { display: flex; flex-wrap: wrap; gap: 10px 16px; font-size: 12px; color: var(--dim);
            padding: 8px 4px 2px; }
  .legend i { display: inline-block; width: 18px; height: 3px; vertical-align: middle;
              margin-right: 5px; border-radius: 2px; }
  h2 { font-size: 14px; margin: 22px 0 8px; color: var(--dim); font-weight: 700;
       letter-spacing: .04em; }
  table { width: 100%; border-collapse: collapse; font-size: 13px; }
  td, th { padding: 7px 6px; border-bottom: 1px solid var(--line); text-align: left; }
  th { color: var(--dim); font-weight: 600; font-size: 12px; }
  td.num { text-align: right; font-variant-numeric: tabular-nums; color: var(--dim); }
  .dot { display: inline-block; width: 8px; height: 8px; border-radius: 50%; margin-right: 6px; }
  .note { font-size: 12px; color: var(--dim); background: #1a2140; border-left: 3px solid var(--fair);
          padding: 9px 11px; border-radius: 0 8px 8px 0; margin: 10px 0; }
  .fail { color: var(--weak); }
  .more { background: none; border: 0; color: var(--accent); font: inherit; font-size: 13px;
          padding: 8px 2px; cursor: pointer; }
  .boot { padding: 40px 16px; text-align: center; color: var(--dim); }
  .spin { display: inline-block; width: 12px; height: 12px; margin-right: 7px;
          border: 2px solid var(--line); border-top-color: var(--accent); border-radius: 50%;
          animation: sp .8s linear infinite; vertical-align: -2px; }
  @keyframes sp { to { transform: rotate(360deg); } }
</style>
</head>
<body>
<header>
  <h1 id="title">📡 メッシュ</h1>
  <div class="netinfo" id="netinfo"></div>
</header>
<main id="main" hidden>
  <div class="bar">
    <button class="act" id="refresh">更新</button>
    <label class="chk"><input type="checkbox" id="hideother"> 名無しを隠す</label>
    <span class="stamp" id="stamp"></span>
  </div>

  <div class="card" id="card">
    <svg id="graph" viewBox="0 0 720 720" aria-label="メッシュ図"></svg>
    <div class="overlay" id="overlay" hidden></div>
    <div class="legend">
      <span><i style="background:var(--good)"></i>強い</span>
      <span><i style="background:var(--fair)"></i>ふつう</span>
      <span><i style="background:var(--weak)"></i>弱い</span>
      <span><i style="background:var(--bad)"></i>ほぼ不通</span>
      <span>● うちの機器 ・ 名無しの参加者 ★ leader ◆ 名前付きの他人</span>
    </div>
  </div>

  <div class="note" id="dupnote" hidden></div>

  <h2>弱いリンク</h2>
  <table id="weak"><thead><tr><th>リンク</th><th class="num">LQI</th><th class="num">RSSI</th><th class="num">誤り率</th></tr></thead><tbody></tbody></table>
  <button class="more" id="weakmore" hidden></button>

  <h2>問い合わせに失敗した機器</h2>
  <table id="failed"><thead><tr><th>機器</th><th>理由</th></tr></thead><tbody></tbody></table>
</main>
<div class="boot" id="boot"><span class="spin"></span>読み込み中…</div>
<script>
const SVGNS = "http://www.w3.org/2000/svg";
const CX = 360, CY = 360, R_IN = 175, R_OUT = 300;
const GCOLOR = { good: "#45c17a", fair: "#e0a53a", weak: "#e4604f", bad: "#a02a1f", route_only: "#3b4770" };
const GWIDTH = { good: 2.4, fair: 1.7, weak: 1.2, bad: 1.0, route_only: 0.8 };
const GORDER = { good: 0, fair: 1, route_only: 1, weak: 2, bad: 3 };
const ERROR_JA = {
  timeout: "時間内に終わりませんでした",
  rejected: "機器が拒否しました",
  network_error: "ネットワーク異常",
  spawn_failed: "コマンドを実行できませんでした",
  failed: "コマンドが失敗しました",
  bad_json: "応答を解釈できませんでした",
};
const PROBE_JA = {
  timeout: "応答なし",
  unreachable: "届かない",
  device_rejected: "機器が拒否",
  store_missing: "認証情報が見つからない",
};

let hideOther = false;
let view = null;      // 直近の GET /api/mesh の応答
let pollTimer = null;

async function api(method, path) {
  const res = await fetch(path, { method });
  if (!res.ok) throw new Error(String(res.status));
  return res.status === 202 ? null : res.json();
}

/* ── レイアウト ───────────────────────────────────────── */
/* リング上の並び順を隣接ノードの平均角へ寄せて交差を減らす（barycenter）。 */
function orderRing(ids, adj, fixedAngle) {
  if (!ids.length) return new Map();
  let cur = ids.slice();
  let ang = new Map(cur.map((id, i) => [id, (i / cur.length) * Math.PI * 2]));
  for (let it = 0; it < 30; it++) {
    const next = new Map();
    for (const id of cur) {
      let x = 0, y = 0, n = 0;
      for (const nb of adj.get(id) || []) {
        const a = ang.has(nb) ? ang.get(nb) : fixedAngle(nb);
        if (a == null) continue;
        x += Math.cos(a); y += Math.sin(a); n++;
      }
      next.set(id, n ? Math.atan2(y, x) : ang.get(id));
    }
    cur = cur.slice().sort((p, q) => next.get(p) - next.get(q));
    ang = new Map(cur.map((id, i) => [id, (i / cur.length) * Math.PI * 2]));
  }
  return ang;
}

function layout(snap) {
  const shown = snap.nodes.filter((n) => !(hideOther && n.kind === "anonymous"));
  const ids = new Set(shown.map((n) => n.id));
  const adj = new Map(shown.map((n) => [n.id, []]));
  for (const e of snap.edges) {
    if (!ids.has(e.a) || !ids.has(e.b)) continue;
    adj.get(e.a).push(e.b);
    adj.get(e.b).push(e.a);
  }
  const pos = new Map();
  const leader = shown.find((n) => n.is_leader);
  if (leader) pos.set(leader.id, { x: CX, y: CY, a: null });

  const inner = shown
    .filter((n) => n !== leader && (n.role === "router" || n.role === "reed"))
    .map((n) => n.id);
  const outer = shown
    .filter((n) => n !== leader && !(n.role === "router" || n.role === "reed"))
    .map((n) => n.id);

  const aIn = orderRing(inner, adj, () => null);
  for (const [id, a] of aIn) pos.set(id, { x: CX + R_IN * Math.cos(a), y: CY + R_IN * Math.sin(a), a });
  const aOut = orderRing(outer, adj, (id) => (aIn.has(id) ? aIn.get(id) : null));
  for (const [id, a] of aOut) pos.set(id, { x: CX + R_OUT * Math.cos(a), y: CY + R_OUT * Math.sin(a), a });
  return pos;
}

/* ── 描画 ─────────────────────────────────────────────── */
function el(name, attrs, text) {
  const e = document.createElementNS(SVGNS, name);
  for (const k in attrs) e.setAttribute(k, attrs[k]);
  if (text != null) e.textContent = text;
  return e;
}

function drawGraph(snap) {
  const svg = document.getElementById("graph");
  svg.innerHTML = "";
  const pos = layout(snap);
  const byId = new Map(snap.nodes.map((n) => [n.id, n]));

  for (const r of [R_IN, R_OUT]) {
    svg.appendChild(el("circle", { cx: CX, cy: CY, r, fill: "none",
      stroke: "#1f2745", "stroke-dasharray": "3 6" }));
  }

  // 弱い線を後（＝上）に描き、良い線に埋もれさせない。
  const drawn = snap.edges
    .filter((e) => pos.has(e.a) && pos.has(e.b))
    .sort((p, q) => GORDER[p.grade] - GORDER[q.grade]);
  for (const e of drawn) {
    const A = pos.get(e.a), B = pos.get(e.b);
    const dim = byId.get(e.a).kind === "anonymous" || byId.get(e.b).kind === "anonymous";
    svg.appendChild(el("line", {
      x1: A.x, y1: A.y, x2: B.x, y2: B.y,
      stroke: GCOLOR[e.grade], "stroke-width": GWIDTH[e.grade],
      "stroke-opacity": dim ? 0.22 : 0.85,
      "stroke-dasharray": e.grade === "bad" || e.grade === "route_only" ? "3 4" : "",
    }));
  }

  for (const n of snap.nodes) {
    const p = pos.get(n.id);
    if (!p) continue;
    const failed = n.probed === false;
    const r = n.kind === "ours" ? 15 : n.kind === "named" ? 12 : 5;
    const fill = failed ? "#3a3f55" : n.kind === "ours" ? "#2b3a66" : n.kind === "named" ? "#3a2f52" : "#2a3150";
    const stroke = failed ? "#7d8296" : n.kind === "ours" ? "#6aa9ff" : n.kind === "named" ? "#b48bdd" : "#4a5479";
    const g = el("g", {});
    g.appendChild(el("circle", { cx: p.x, cy: p.y, r, fill, stroke,
      "stroke-width": n.kind === "ours" ? 2 : 1.2,
      "stroke-dasharray": failed ? "3 3" : "" }));
    if (n.is_leader) {
      g.appendChild(el("text", { x: p.x, y: p.y + 4.5, "text-anchor": "middle",
        "font-size": 12, fill: "#ffd76a" }, "★"));
    } else if (n.kind === "named") {
      g.appendChild(el("text", { x: p.x, y: p.y + 4, "text-anchor": "middle",
        "font-size": 11, fill: "#e8ecf8" }, "◆"));
    }
    if (n.kind !== "anonymous" || n.is_leader) {
      const anchor = p.a == null ? "middle"
        : Math.cos(p.a) < -0.15 ? "end" : Math.cos(p.a) > 0.15 ? "start" : "middle";
      const dx = p.a == null ? 0 : Math.cos(p.a) * (r + 6);
      const dy = p.a == null ? -(r + 10) : Math.sin(p.a) * (r + 6) + 4;
      g.appendChild(el("text", { x: p.x + dx, y: p.y + dy, "text-anchor": anchor,
        "font-size": 11, fill: failed ? "#7d8296" : "#cdd6f0" }, n.name));
      if (n.role === "sed" || n.role === "reed") {
        g.appendChild(el("text", { x: p.x + dx, y: p.y + dy + 12, "text-anchor": anchor,
          "font-size": 9.5, fill: "#8b96b8" }, n.role === "sed" ? "電池・間欠" : "中継しない"));
      }
    }
    svg.appendChild(g);
  }
}

/* ── 表・ヘッダ ───────────────────────────────────────── */
let weakExpanded = false;

function drawWeak(snap) {
  const byId = new Map(snap.nodes.map((n) => [n.id, n]));
  const rows = snap.edges
    .filter((e) => e.grade === "weak" || e.grade === "bad")
    .sort((p, q) => GORDER[q.grade] - GORDER[p.grade] || p.lqi - q.lqi || p.rssi - q.rssi);
  const tb = document.querySelector("#weak tbody");
  tb.innerHTML = "";
  const shown = weakExpanded ? rows : rows.slice(0, 10);
  for (const e of shown) {
    const tr = document.createElement("tr");
    const td = (h) => { const c = document.createElement("td"); c.innerHTML = h; return c; };
    const name = document.createElement("td");
    const dot = document.createElement("span");
    dot.className = "dot";
    dot.style.background = GCOLOR[e.grade];
    name.appendChild(dot);
    name.appendChild(document.createTextNode(
      `${byId.get(e.a).name} ↔ ${byId.get(e.b).name}`));
    tr.appendChild(name);
    for (const v of [e.lqi, `${e.rssi} dBm`, `${e.fer}%`]) {
      const c = td("");
      c.className = "num";
      c.textContent = v;
      tr.appendChild(c);
    }
    tb.appendChild(tr);
  }
  if (!rows.length) {
    const tr = document.createElement("tr");
    const c = document.createElement("td");
    c.colSpan = 4;
    c.style.color = "var(--dim)";
    c.textContent = "弱いリンクはありません";
    tr.appendChild(c);
    tb.appendChild(tr);
  }
  const more = document.getElementById("weakmore");
  more.hidden = rows.length <= 10;
  more.textContent = weakExpanded ? "折りたたむ" : `他 ${rows.length - 10} 件を表示`;
}

function drawFailed(snap) {
  const tb = document.querySelector("#failed tbody");
  tb.innerHTML = "";
  const failed = snap.nodes.filter((n) => n.probed === false);
  if (!failed.length) {
    const tr = document.createElement("tr");
    const c = document.createElement("td");
    c.colSpan = 2;
    c.style.color = "var(--dim)";
    c.textContent = "なし";
    tr.appendChild(c);
    tb.appendChild(tr);
    return;
  }
  for (const n of failed) {
    const tr = document.createElement("tr");
    const a = document.createElement("td");
    a.className = "fail";
    a.textContent = n.name;
    const b = document.createElement("td");
    b.textContent = PROBE_JA[n.error_kind] || n.error_kind || "不明";
    tr.appendChild(a);
    tr.appendChild(b);
    tb.appendChild(tr);
  }
}

function drawHeader(v) {
  const snap = v.snapshot;
  document.getElementById("title").textContent = v.label ? `📡 ${v.label}` : "📡 メッシュ";
  const info = document.getElementById("netinfo");
  if (!snap) { info.textContent = ""; return; }
  const ours = snap.nodes.filter((n) => n.kind === "ours").length;
  const parts = [
    `ネットワーク <b>${snap.network.name ?? "?"}</b>`,
    `チャンネル <b>${snap.network.channel ?? "?"}</b>`,
    `機器 <b>${ours}</b> / 参加者 <b>${snap.nodes.length}</b>`,
    snap.network.split
      ? `<span class="warn">⚠ メッシュが分裂しています</span>`
      : `分裂 <b>なし</b>`,
  ];
  info.innerHTML = parts.join("");
}

function drawStamp(v) {
  const stamp = document.getElementById("stamp");
  if (v.status === "running") {
    const sec = Math.round((v.elapsed_ms ?? 0) / 1000);
    const n = v.snapshot ? v.snapshot.nodes.filter((x) => x.kind === "ours").length : null;
    const who = n ? `${n} 台` : "各機器";
    stamp.innerHTML = `<span class="spin"></span>${who}に順に問い合わせています… 経過 ${sec} 秒（ふつう 2 分ほど）`;
  } else if (v.age_ms != null) {
    const m = Math.floor(v.age_ms / 60000);
    stamp.textContent = m < 1 ? "たった今取得" : `${m} 分前に取得`;
  } else {
    stamp.textContent = "";
  }
}

function render(v) {
  view = v;
  document.getElementById("boot").hidden = true;
  document.getElementById("main").hidden = false;
  document.getElementById("refresh").disabled = v.status === "running";
  drawHeader(v);
  drawStamp(v);

  const card = document.getElementById("card");
  const overlay = document.getElementById("overlay");
  card.classList.toggle("stale", v.status === "running" && !!v.snapshot);

  if (v.snapshot) {
    drawGraph(v.snapshot);
    drawWeak(v.snapshot);
    drawFailed(v.snapshot);
    const note = document.getElementById("dupnote");
    note.hidden = !v.snapshot.unidentified_count;
    note.textContent =
      `うちの機器 ${v.snapshot.unidentified_count} 台は自分の識別子を名乗れませんでした` +
      `（同一 ID を返すファームウェアの既知問題）。そのぶん、同じ機器が「名無しの参加者」` +
      `としても図に出ている可能性があります。推測で統合はしません。`;
  } else {
    document.getElementById("graph").innerHTML = "";
  }

  overlay.hidden = true;
  if (v.status === "running" && !v.snapshot) {
    overlay.hidden = false;
    overlay.innerHTML = `<div><span class="spin"></span>各機器に順に問い合わせています…<br>ふつう 2 分ほどかかります</div>`;
  } else if (v.status === "failed" && !v.snapshot) {
    overlay.hidden = false;
    overlay.innerHTML = `<div>メッシュを調べられませんでした<br><span style="color:var(--dim)">${ERROR_JA[v.error] || v.error || ""}</span></div>`;
  } else if (v.status === "failed") {
    overlay.hidden = false;
    overlay.innerHTML = `<div>更新できませんでした<br><span style="color:var(--dim)">${ERROR_JA[v.error] || v.error || ""}（表示は前回の結果）</span></div>`;
  } else if (v.status === "empty") {
    overlay.hidden = false;
    overlay.innerHTML = `<div>まだ調べていません<br><span style="color:var(--dim)">「更新」を押すと調べます</span></div>`;
  }
}

/* ── 取得ループ ───────────────────────────────────────── */
async function poll() {
  let v;
  try {
    v = await api("GET", "/api/mesh");
  } catch (e) {
    document.getElementById("boot").hidden = false;
    document.getElementById("boot").textContent = "⚠ サーバに接続できません";
    return;
  }
  render(v);
  clearTimeout(pollTimer);
  if (v.status === "running") {
    pollTimer = setTimeout(poll, 5000);
  } else if (v.stale) {
    // 未取得 or ttl 超過 → 自動で仕掛ける（見ている間だけ pull）
    await api("POST", "/api/mesh/refresh").catch(() => {});
    pollTimer = setTimeout(poll, 1000);
  }
}

document.getElementById("refresh").addEventListener("click", async () => {
  document.getElementById("refresh").disabled = true;
  await api("POST", "/api/mesh/refresh").catch(() => {});
  setTimeout(poll, 300);
});
document.getElementById("hideother").addEventListener("change", (ev) => {
  hideOther = ev.target.checked;
  if (view && view.snapshot) drawGraph(view.snapshot);
});
document.getElementById("weakmore").addEventListener("click", () => {
  weakExpanded = !weakExpanded;
  if (view && view.snapshot) drawWeak(view.snapshot);
});
poll();
</script>
</body>
</html>
```

- [ ] **Step 2: `/mesh` が mesh.html を返すテストを書く**

`src/main.rs` の `mod tests` に追加する。

```rust
    #[tokio::test]
    async fn mesh_page_is_served_when_configured() {
        let app = app_from(&format!(
            "{MESH_DEVICE}\n[mesh]\ncommand = {MESH_OK_CMD}\n"
        ));
        let res = router(app)
            .oneshot(
                Request::builder()
                    .method("GET")
                    .uri("/mesh")
                    .body(Body::empty())
                    .unwrap(),
            )
            .await
            .unwrap();
        assert_eq!(res.status(), StatusCode::OK);
        let bytes = res.into_body().collect().await.unwrap().to_bytes();
        let body = String::from_utf8(bytes.to_vec()).unwrap();
        assert!(body.contains("<title>メッシュ — mando</title>"));
        assert!(body.contains("/api/mesh/refresh"));
    }
```

- [ ] **Step 3: テストが落ちることを確認**

Run: `cargo test mesh_page_is_served_when_configured`
Expected: FAIL（Task 3 の暫定 `mesh_page` は空文字を返すので `<title>` を含まない）

- [ ] **Step 4: `mesh.html` を焼き込む**

`src/main.rs` の `INDEX_HTML` の定義の直後に追加する。

```rust
/// メッシュ表示の UI（[mesh] 未設定なら配らない）。index.html とは独立した画面。
const MESH_HTML: &str = include_str!("../mesh.html");
```

Task 3 で置いた暫定 `mesh_page` の本体を差し替える。

```rust
async fn mesh_page(State(app): State<Shared>) -> Response {
    if app.config.mesh.is_none() {
        return mesh_not_configured();
    }
    Html(MESH_HTML).into_response()
}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `cargo test mesh_`
Expected: 全 PASS（Task 2・3 のぶんを含む）

- [ ] **Step 6: 全テストと clippy**

Run: `cargo test && cargo clippy --all-targets -- -D warnings`
Expected: すべて PASS、警告なし

- [ ] **Step 7: 実際に動かして目視確認**

`config.toml` に偽の mesh コマンドを一時的に足して起動する（実機を叩かずに UI を確認する）。

```bash
cat > /tmp/mando-mesh-check.toml <<'EOF'
bind = "127.0.0.1:8099"

[[device]]
name = "s1"
get_state = ["sh", "-c", "printf '{}'"]
open  = ["sh", "-c", "printf '{}'"]
close = ["sh", "-c", "printf '{}'"]

[mesh]
label = "テストメッシュ"
command = ["sh", "-c", "sleep 3; printf '{\"timestamp\":\"t1\",\"network\":{\"name\":\"test-thread\",\"channel\":15,\"partition_ids\":[1],\"leader_router_id\":38},\"nodes\":[{\"id\":\"node:5\",\"node_id\":5,\"alias\":\"desk_light\",\"role\":\"router\",\"probed\":true},{\"id\":\"node:6\",\"node_id\":6,\"alias\":\"hall_light\",\"role\":\"router\",\"probed\":true},{\"id\":\"node:7\",\"node_id\":7,\"alias\":\"study_motion\",\"role\":\"sed\",\"probed\":true},{\"id\":\"node:8\",\"node_id\":8,\"role\":\"unknown\",\"probed\":false,\"probe_error\":{\"kind\":\"timeout\"}},{\"id\":\"ext:0011223344556677\",\"ext_address\":\"0011223344556677\",\"role\":\"leader\"},{\"id\":\"ext:8899AABBCCDDEEFF\",\"ext_address\":\"8899AABBCCDDEEFF\",\"role\":\"router\",\"label\":\"otbr-br\"},{\"id\":\"ext:AABBCCDDEEFF0011\",\"ext_address\":\"AABBCCDDEEFF0011\",\"role\":\"router\"}],\"edges\":[{\"a\":\"node:5\",\"b\":\"node:6\",\"a_sees_b\":{\"lqi\":3,\"avg_rssi\":-60,\"frame_error_rate\":0},\"b_sees_a\":null},{\"a\":\"node:5\",\"b\":\"ext:0011223344556677\",\"a_sees_b\":{\"lqi\":1,\"avg_rssi\":-94,\"frame_error_rate\":88},\"b_sees_a\":null},{\"a\":\"node:6\",\"b\":\"ext:8899AABBCCDDEEFF\",\"a_sees_b\":{\"lqi\":2,\"avg_rssi\":-78,\"frame_error_rate\":2},\"b_sees_a\":null},{\"a\":\"node:7\",\"b\":\"node:6\",\"a_sees_b\":{\"lqi\":0,\"avg_rssi\":-99,\"frame_error_rate\":0},\"b_sees_a\":null},{\"a\":\"node:5\",\"b\":\"ext:AABBCCDDEEFF0011\",\"a_sees_b\":null,\"b_sees_a\":null,\"route\":{\"path_cost\":1}}]}'"]
EOF
MANDO_CONFIG=/tmp/mando-mesh-check.toml cargo run &
sleep 3
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8099/mesh
```

ブラウザ（またはヘッドレス）で `http://127.0.0.1:8099/mesh` を開き、次を確認する:

- 開いた直後に自動で取得が始まり、「各機器に順に問い合わせています…」が出る
- 3 秒後に図が描かれる。★ が中心、◆ が otbr-br、うちの機器 3 台が名前付きで出る
- 弱いリンク表に「desk light ↔ 0011（LQI 1 / -94 dBm / 88%）」と「study motion ↔ hall light（LQI 0）」が出る
- 失敗した機器の表に node:8 が「応答なし」で出る
- 「名無しを隠す」で `ext:AABB…` が消える
- 「更新」でもう一度取得が走る

確認後、プロセスを止める:
```bash
kill %1
rm -f /tmp/mando-mesh-check.toml
```

- [ ] **Step 8: コミット**

```bash
git add mesh.html src/main.rs
git commit -m "feat(ui): /mesh — 役割同心円のメッシュ図・弱いリンク一覧（#5）"
```

---

## Task 5: README の更新

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: Task 1〜4 の成果
- Produces: なし

- [ ] **Step 1: README を読んで API 一覧の場所を確認**

Run: `grep -n "api/health\|api/graphs" README.md`
Expected: 安定ミニ API の一覧が載っている行番号が出る

- [ ] **Step 2: mesh の項を追記**

`/api/health` を説明している箇所の直後に、同じ書式で追記する。

```markdown
- `GET  /mesh` — Thread メッシュの表示画面（`[mesh]` 未設定なら 404）。トップからはリンクしない
- `GET  /api/mesh` — 取得ジョブの状態とスナップショット（即答）
- `POST /api/mesh/refresh` — 取得ジョブを起動（202・single-flight）

> `mat diag mesh` は全ノードを逐次 probe するので重い（13 ノードで実測 1 分 45 秒）。
> 取得は非同期ジョブで走り、`/mesh` を開いている間だけ動く。誰も見ていなければ
> 何も走らない（mando はスケジューラを持たない）。
```

- [ ] **Step 3: コミット**

```bash
git add README.md
git commit -m "docs: /mesh と mesh API を README に追記（#5）"
```

---

## Self-Review 結果

**1. スペック網羅**

| スペックの節 | 実装するタスク |
|---|---|
| §1 config `[mesh]` | Task 1 |
| §2 API（4 状態・single-flight・レーン・メモリ内 1 スロット） | Task 3 |
| §3 正規化・grade | Task 2 |
| §4 画面（図・取得中・下部・エラー） | Task 4 |
| §5 テスト | Task 1・2・3・4 の各 Step |
| §6 やらないこと | 実装しないことの確認のみ（コード不要） |
| §7 issue | 計画作成前に作成済み（mando#5 / mat#13） |

スペック §3 の `is_border_router` と grade 判定順序は、冒頭「設計からの意図的な逸脱」に理由付きで記載し、Task 2 に反映した。

**2. プレースホルダ**: なし。各ステップに実際のコード・実際のコマンド・期待する出力が入っている。

**3. 型の一貫性**: `MeshThresholds`（config 定義・normalize が参照）、`MeshView` / `MeshNode` / `MeshEdge`（normalize 定義・main が参照）、`MeshJob`（main 定義・テストが参照）、`app_from` / `call_on` / `settle` / `MESH_DEVICE` / `MESH_OK_CMD`（Task 3 で定義・Task 4 が参照）。すべて定義と参照が一致している。
